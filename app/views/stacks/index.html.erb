<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl mb-6">Stacks</h1>
    <%= link_to 'New stack', new_stack_path, class: "btn btn-primary mb-6" %>
  </div>

  <div id="stacks" class="grid grid-cols-1 md:grid-cols-2 gap-10">
    <%= render @stacks %>
  </div>
</div>
