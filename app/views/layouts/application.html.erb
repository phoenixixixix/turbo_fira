<!DOCTYPE html>
<html>
  <head>
    <title>TurboFira</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if notice.present? %>
      <div class="container mx-auto max-w-3xl relative px-4">
        <div class="absolute inset-0 pt-2 px-4">
          <div class="alert bg-base-100 border-0 shadow">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-info shrink-0 w-6 h-6"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            <span><%= notice %></span>
          </div>
        </div>
      </div>
    <% end %>

    <main class="container mx-auto max-w-3xl pt-24 px-4">
      <%= yield %>
    </main>
  </body>
</html>
